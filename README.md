# gnn-link-prediction-pubmed
Implemented GNN-based link prediction on the PubMed citation network using GCN, GraphSAGE, GAT, and VGAE. Built a controlled pipeline with edge splits and negative sampling, evaluated via ROC-AUC/AP, and performed robustness analysis (feature masking, edge dropout) on top models.
